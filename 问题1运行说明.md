# 问题 1 可复现实验

本目录的求解器针对场景 A。它读取附件中的原始计算图和固定 `config.txt`，生成 `node_to_subgraph`、`core_schedules`，并调用附件提供的 `evaluate_scene_a` 选择 Makespan 最小的候选方案。

## 环境

- Python 3.9 或更新版本；求解器、批量脚本和赛题评测器只使用 Python 标准库。
- 保持本目录下的 `通用神经网络处理器下的多核调度问题  附件/code`、`data` 和两个求解器文件的相对位置。
- 不修改附件中的 `data/config.txt`。

## 单个方案

在本目录执行：

```bash
python3 A题_problem1_solver_v4_2.py \
  '通用神经网络处理器下的多核调度问题  附件/data/case_001.json' \
  -n 5 \
  --config '通用神经网络处理器下的多核调度问题  附件/data/config.txt' \
  -o case_001_multicore_res.json
```

终端打印所选候选、单核基准、Makespan、加速比和候选评测记录。输出的方案文件也可传给附件中的问题 1 评测脚本独立复核：

```bash
python3 '通用神经网络处理器下的多核调度问题  附件/code/multicore_cut_evaluate_problem_1.py' \
  '通用神经网络处理器下的多核调度问题  附件/data/case_001.json' \
  case_001_multicore_res.json \
  --config '通用神经网络处理器下的多核调度问题  附件/data/config.txt'
```

## 全部用例与核数

```bash
python3 run_problem1_experiments.py --workers 4
```

若先复现操作和边较少的用例，可按输入 JSON 的 `ops`、`edges` 列表长度筛选：

```bash
python3 run_problem1_experiments.py \
  --max-ops 5000 --max-edges 12000 \
  --out results/problem1_small_5000ops_12000edges \
  --workers 4
```

这组条件选中 55 个用例；`selection.csv` 逐例记录操作数、边数及是否入选。
完成后可使用安装了 Matplotlib 的 Python 绘制 1～5 核平均加速比曲线；当前机器可直接运行：

```bash
/opt/anaconda3/bin/python3 plot_problem1_summary.py \
  results/problem1_small_5000ops_12000edges/summary.csv
```

包含中等规模图的扩展实验选中 85 个用例，暂缓其余 15 个更大的图（包括 `case_014`）：

```bash
python3 run_problem1_experiments.py \
  --max-ops 15000 --max-edges 40000 \
  --out results/problem1_upto15000ops_40000edges \
  --workers 4

/opt/anaconda3/bin/python3 plot_problem1_summary.py \
  results/problem1_upto15000ops_40000edges/summary.csv
```

默认读取 `data/case_001.json` 至 `case_100.json`，对每个用例运行 2、3、4、5 核；1 核由官方单核方案作为基准，加速比定义为 1。运行中可中断，同一命令会跳过已有且完整的成功结果，继续未完成项。重跑已有结果需加 `--force`。
同一用例的单核评测结果缓存在 `singlecore_result.json`，供各核数复用。求解器对超过 10000 个核内操作的大图使用较小的候选组合，仍由官方评测器对候选方案打分。

输出写入 `results/problem1_v4_2/`：

- `summary.csv`：按核数汇总平均加速比、完成用例数和总耗时；平均加速比是逐例加速比的算术平均值。
- `per_case.csv`：各用例、各核数的 Makespan、加速比、额外搬运量和运行时间。
- `singlecore_baseline.csv`：各用例的单核基准 Makespan。
- `<case>/<n>cores/plan.json`：提交给官方评测器的方案。
- `<case>/<n>cores/official_result.json`：所选方案由官方问题 1 评测函数返回的完整结果。
- `<case>/<n>cores/solver_info.json`：所有候选的评测记录与所选方案。
- `<case>/<n>cores/metrics.json`：该次任务的简要记录；失败时包含错误堆栈。
- `<case>/singlecore_result.json`：官方单核评测器结果。

例如只运行两个用例的双核与五核结果：

```bash
python3 run_problem1_experiments.py --cases case_001 case_002 --cores 2 5 --workers 2
```
